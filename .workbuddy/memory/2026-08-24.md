# 2026-08-24

## ChemMate AI · MVP V1.0 从 0 到上线

### 今天完成
- 初始化 Vite + React 18 + TypeScript + Tailwind CSS 项目骨架
- 实现三个页面：/ 首页、/chat AI 聊天、/about 产品介绍
- 首页含 Hero（含 AI 对话 mockup）、三大功能卡片、三步流程
- 聊天页：先以 mock 模式实现完整 UX（欢迎态 / 输入框 / 加载态 / 错误态 / LaTeX 公式 / 重试）
- 封装 src/services/dify.ts service 层（mock 模式 + 预留真实 API 接口）
- 配置 Vercel 部署、vercel.json、.env.example、.gitignore
- 加 LaTeX 公式渲染（KaTeX）
- 加 SEO meta、favicon
- 写 README.md（含项目结构、关键文件对照表、Dify 接入指南）

### 关键决策
- 桌面端聊天区最大宽度 1100px（姐姐后面会改这个数）
- 整体设计：橙色 #F97316 + 白色 + 留白 + 细圆角（8-14px）+ 轻阴影

### 后改为 iframe 嵌入
- 姐姐要求用 iframe 替代 Dify API（更安全、零后端）
- 重写 src/pages/Chat.tsx，嵌入 https://udify.app/chatbot/1MWwVdrO3gbKB9b0
- 加 loading 态（旋转 spinner）+ 错误态（重新加载按钮）
- iframe 高度策略：flex-1 + min-height 700px，桌面 = 视口高 - Header - padding
- 桌面最大 1100px 居中（tailwind maxWidth.chat 改 1100px）
- 移动端：width 100%、padding 减小，无横向滚动

### 验证
- TypeScript 零错误
- Vite build 干净通过
- Bundle 体积：iframe 模式 177KB / gzip 57KB（去掉 markdown 依赖后）
- Dify URL 200 可达

### 当前状态
- dev server 跑在 http://localhost:5173/
- 三个页面都能访问
- 聊天页 iframe 已嵌入并显示 loading 态
- 备用组件（useChat / ChatMessage / ChatInput 等）暂未删除，留作未来切换 API 模式时复用

### 待办（V2 候选）
- [ ] 真实部署到 Vercel
- [ ] 域名绑定
- [ ] 移动端真机测试
- [ ] 是否清理未使用的 chat 组件
