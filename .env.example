# ---- 前端可暴露给浏览器的变量（VITE_ 前缀） ----
# 后端代理 API 地址（开发时指向本地 serverless function）
VITE_API_BASE_URL=/api

# ---- 后端变量（不会暴露给前端，仅在 serverless function 中使用） ----
# Dify API Key（在 Dify 应用后台获取）
DIFY_API_KEY=your-dify-api-key

# Dify 基础地址（不带末尾斜杠）
DIFY_BASE_URL=https://api.dify.ai/v1
